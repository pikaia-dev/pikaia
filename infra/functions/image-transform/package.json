{
    "name": "@tango/image-transform-lambda",
    "version": "1.0.0",
    "description": "Lambda@Edge function for CloudFront image transformation using Sharp",
    "main": "index.js",
    "type": "commonjs",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "build": "npm install --omit=dev --platform=linux --arch=x64"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.700.0",
        "sharp": "^0.33.5"
    },
    "devDependencies": {
        "vitest": "^2.0.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}