/**
 * Shared types used across multiple features.
 */

// Re-export commonly used types from lib/api for convenience
export type {
  BillingAddress,
  MeResponse,
  OrganizationDetail,
  SubscriptionInfo,
  UserInfo,
} from '@/lib/api'
